# Used only by the loopback demo worker. Never expose this value to the browser.
AKASHML_API_KEY=

# GLM 5.2 uses AkashML's Anthropic-endpoint alias for its upstream model ID.
AKASHML_MODEL=zai-org--GLM-5.2
AKASHML_API_BASE=https://api.akashml.com
MERGESTAMP_DEMO_PORT=4317
